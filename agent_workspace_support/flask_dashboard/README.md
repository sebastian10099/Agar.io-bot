# Flask Dashboard Locally Run on Non-Default Ports

## How to Start:
1. Install Flask if not already installed: `pip install flask`
2. Navigate to the directory containing your Flask application.
3. Execute the script: `/root/local_agent/agent_workspace_support/flask_dashboard/start_flask.sh`