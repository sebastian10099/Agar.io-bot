# Start des Flask-Dashboards

Derzeit ist das Flask-Dashboard nicht verfügbar. Das Paket `gunicorn` wurde nicht installiert, da 'pip' und 'grep' fehlen. Die Installation dieser Tools wird als nächstes Schritt in die Planung aufgenommen.