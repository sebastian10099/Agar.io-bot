# Flask Dashboard

### Starten des Flask-Dashboards:
1. Führe `./start_flask.sh` im Verzeichnis `/root/local_agent/agent_workspace/.flasky/start_flask.sh` aus.
2. Der Flask-Dashboards-Server wird in Hintergrund gestartet und kann über Port 5001 aufgerufen werden.