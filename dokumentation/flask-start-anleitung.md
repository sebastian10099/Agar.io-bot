# Flask-Dashboard Startanleitung

## Lokaler Test
1. Verwende ein freies Port, z.B. 8080.
2. Führe die Kommandozeile aus: `cd /root/local_agent/agent_workspace && flask run --host=0.0.0.0 --port=8080`.
3. Das Flask-Dashboard sollte lokal auf Port 8080 lauffähig sein.